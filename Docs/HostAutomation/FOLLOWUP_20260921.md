# Issue #52 rollout follow-up — 2026-09-21

The post-merge independent audit found three demonstrated defects in the
implementation merged by PR #53. This follow-up supplies the missing review
input, applies the Issue-author trust requirement, and closes a Windows
process-cleanup failure path. It also fixes a timing-dependent PlayMode test
failure observed during this validation. This is implementation evidence;
installation, real-model smoke, the product pilot and schedule activation
remain separate rollout steps.

Runtime/test commit: `8ff1891c62c3943b732f23c38d6c7e6edeb95ac9`.
Base/main: `528452fd22a9ac9f722c41f565a7351e8ed292b3`.
SPEC_VERSION: `1.0.5`; blob
`90a27f9cea6e8f02e05a8bbab5d14650e3e932af`.

## Corrections

- The Host now supplies the exact pinned-main-to-synthetic-commit changed-path
  list and text diff to the production Reviewer. Before/after lines, deleted
  files and rename-as-delete/add paths are available even though source MCP
  exposes only the current tree. Binary entries are explicit. The Host refuses
  inconsistent, sensitive or oversized context before model dispatch; the
  diff JSON limit is 1 MiB and the complete prompt limit is 2 MiB. Neither is
  truncated, and the temporary prompt is still removed after execution.
- GitHub Issue authors are read and checked against the configured allowlist
  before selecting any role/mode. Missing/null/empty/unknown identities are
  rejected. The fresh publication contract repeats the check, and the author
  participates in the immediate-before-mutation snapshot comparison.
- A suspended child is entered in the PID/start-time ledger before Windows
  job assignment. If assignment or that callback fails, the Host terminates
  the child through its already owned process handle and waits. Confirmed
  termination clears the matching ledger entry; unconfirmed termination
  retains the identity for recovery. Successfully assigned jobs keep their
  existing cleanup path.
- `AudioClockPlayModeTests.RuntimeClip_ExercisesFullTransportAndReplay` now
  schedules its fixture 250 ms ahead, as the existing scheduled-stop test
  does. The previous 20 ms margin could expire after a 21.333 ms DSP advance
  between adjacent assertions. All behavior assertions remain. Game code,
  judgement windows, BPM, scenes, assets and serialized settings are unchanged.

## Executed verification

Evidence is retained locally under `Logs/HostRollout52-20260921` outside the
tracked source. The implementation and independent review used separate
standalone clones; the user's Base/Developer/Reviewer checkouts were not edited.

- Host regression: **104 distinct cases PASS by their latest executed result**. The complete run returned 103 PASS / 1 test-harness timeout at 60 seconds. That stale-head case, compiler boundary and mutation audit then passed 3/3 with a 120-second fixture limit; production timeouts were unchanged. The stale-head retest took 44.697 seconds. Both runs recorded zero external mutations. `followup-coverage.json` maps every case to its actual result file.
- Independent review of the exact runtime commit: **Blocker 0 / Major 0 /
  Minor 0**. See `INDEPENDENT_FOLLOWUP_20260921.md`. Independent executions were
  baseline 100 cases, the unchanged diff/author fix's six-case group, and the
  final eight-case process group, all PASS. These counts overlap and are not
  added together as distinct tests. Additional independent real-Git context
  checks and three native failure-injection cases passed.
- Fresh Unity `6000.4.0f1` import/compile: exit 0. EditMode **43/43 PASS** and
  PlayMode **8/8 PASS**, failed/skipped/inconclusive 0. Source and validation
  clones were clean after testing. The normalized ProjectSettings SHA-256
  stayed `1613f4e7d2a0d1dd791836e4791a12ce1805bade48756541ee9ce802d56fe902`.
- Actual Editor inventory: **50 scene/prefab records, zero errors**. Metadata
  scan: **1,264 meta files**, zero missing/orphan/invalid metadata, duplicate
  GUID, Missing Script or broken reference. Eleven `.gitkeep` placeholders
  were explicitly ignored. Compile/inventory/test log diagnostics were empty.
- The inventory also exercised the final common native process boundary with
  the real Unity executable: job assignment and process-tree termination were
  confirmed.
- Both production GraphQL queries retrieved the actual Issue #52 author and
  complete Project field contract successfully. This was read-only, with no
  dispatch, publication or product state transition.
- PowerShell parsing: **20 Host scripts, zero errors**; `git diff --check`
  passed. All 38 LFS objects were SHA-256 checked and materialized for Unity.
- Actual-machine installation preview: PASS, Changed=false, Staged=false,
  TaskEnabled=false. Installer SHA-256:
  `cff0acc1587d6ac75897c688847a7e672e64f289e3411bad7e4cbc46966dc973`.
  Preview BundleId:
  `c6f7777a8547213b572710361ce454abee89f40679a93bb9075aa0c9f117daec`.
  The installer and its transaction logic are unchanged. The previous 170-row
  installation transaction matrix is historical evidence, not a new live
  installation or a rerun in this follow-up.

The native failure cases inject named API return values while creating real
harmless test children. They do not claim that Windows naturally produced
those API failures. Assignment failure and ledger-callback failure left no
child; assignment plus termination failure retained the exact identity, then
the fixture identified and cleaned up only its own child. No child code ran.

Failed attempts are preserved and are not counted as PASS. Initial new fixture
drivers needed a safe optional-property lookup, a supported timeout, and
capture of fake Codex stdin before the production prompt's required deletion.
The independent process-only subset initially lacked a fake Git/GitHub audit
invocation; adding that audit prerequisite yielded its final eight-case PASS.
The first actual Unity run had **7/8 PlayMode PASS** and failed the original
20 ms assertion with SongTimeMs=1.3333333333340747 ms. The final fresh run above
is after the test-only margin correction. A reused Unity directory was refused
before launch by the fresh-clone preflight; a new clean clone was used instead.
Read-only local evidence drivers also had a bracket/dependency-loading error,
which was corrected before their complete successful results were recorded.

## Remaining Owner rollout

PR #53 was merged by the Owner, and GitHub currently shows Issue #52 closed
and its Project card Done. Those states do not establish that the operational
rollout has run. This follow-up does not change them or process a second Issue.

1. Owner reviews and merges this follow-up's exact head. Agents may not merge.
2. Keep all five old Desktop schedules paused. Re-preview the merged source
   and install the reviewed protected bundle with the new task **disabled**.
3. Run the installed environment and actual Developer/Reviewer model smoke,
   including source writes, read-only review, authentication and containment.
4. Establish a legitimate current handoff and run the separate Issue #20 /
   PR #47 pilot through Verification. The current queue's missing handoff is
   not fabricated into a human failure or bypassed.
5. Enable the 15-minute Host schedule only after the pilot passes.

At this report's preparation, all five legacy schedules were PAUSED, the
Windows Host task did not exist, and the protected installation directory did
not exist. No actual install, live model dispatch, pilot, agent merge, or agent
Done transition is claimed here.
